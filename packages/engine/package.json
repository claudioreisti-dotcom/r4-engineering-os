{
  "name": "@r4-eos/engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "R4 EOS Knowledge Engine — deterministic, stateless interpreter of declarative knowledge.",
  "bin": {
    "r4": "src/cli.ts"
  },
  "scripts": {
    "validate": "node src/cli.ts validate",
    "generate": "node src/cli.ts generate",
    "check": "node src/cli.ts generate --check",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
